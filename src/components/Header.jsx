import { useState } from "react";
const links = [{ label: "Work", href: "#experience" }, { label: "Projects", href: "#projects" }, { label: "About", href: "#about" }];
export default function Header({ resumeUrl }) { const [open, setOpen] = useState(false); return <header className="site-header"><nav className="nav-shell" aria-label="Main navigation"><a className="wordmark" href="#top" onClick={() => setOpen(false)}><img className="wordmark-mark" src="/favicon.svg" alt="" />Sahil<span>.</span></a><div className={`nav-links ${open ? "open" : ""}`}>{links.map(link => <a key={link.label} href={link.href} onClick={() => setOpen(false)}>{link.label}</a>)}<a className="nav-resume mobile-resume" href={resumeUrl} target="_blank" rel="noreferrer">Resume ↗</a></div><a className="nav-resume desktop-resume" href={resumeUrl} target="_blank" rel="noreferrer">Resume ↗</a><button className="menu-toggle" type="button" aria-expanded={open} aria-label="Toggle menu" onClick={() => setOpen(!open)}><span /><span /></button></nav></header>; }
